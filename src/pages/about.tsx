import Base from '~/layouts/base'

const About = () => {
  return (
    <Base>
      This is the about page
    </Base>
  )
}

export const head = {
  title: 'About page'
}

export default About